"""
Example alternate rota (not wired): copy this pack layout and implement your own
`hexengine_pack.toml` + `load_game_definition()` returning e.g.
`StaticScheduleGameDefinition(...)` from `hexengine.gamedef.builtin`.
"""
